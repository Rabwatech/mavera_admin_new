# 📊 تقرير تحليل شامل - نظام مافيرا (Mafira) لإدارة الحجوزات

**تاريخ التحليل:** 2024  
**إصدار Next.js:** 14.1.0  
**نوع المشروع:** نظام إدارة حجوزات قاعات وفعاليات (Wedding Hall Management System)

---

## 📁 1. هيكل المشروع

### عدد الصفحات والمسارات (Routes)

**إجمالي الصفحات:** 15 صفحة رئيسية

#### صفحات الإدارة (Admin)
- `/` - الصفحة الرئيسية (إعادة توجيه حسب الدور)
- `/admin` - لوحة تحكم الإدارة (Command Center)
- `/admin/finance` - لوحة المالية
- `/admin/finance/invoices` - إدارة الفواتير
- `/admin/finance/contracts` - أرشيف العقود
- `/admin/finance/integrations` - التكاملات الخارجية

#### صفحات المبيعات (Sales)
- `/sales-dashboard` - لوحة تحكم المبيعات
- `/sales/new` - معالج الحجز الجديد (Booking Wizard)
- `/sales/pipeline` - مراحل البيع (Kanban Board)
- `/sales/tours` - إدارة الجولات والزيارات

#### صفحات أخرى
- `/leads` - إدارة العملاء الجدد (Call Center)
- `/users` - إدارة الموظفين
- `/approvals` - موافقات الخصم
- `/reports` - التقارير والتحليلات
- `/clients` - سجل العملاء الشامل
- `/arrangements` - الترتيبات والتنفيذ

### عدد المكونات (Components)

**إجمالي المكونات:** 15 مكون

#### مكونات التخطيط (Layout)
- `Sidebar.tsx` - القائمة الجانبية مع تصفية حسب الدور
- `Header.tsx` - شريط التنقل العلوي مع البحث وتبديل اللغة

#### مكونات الحجوزات (Booking)
- `BookingWizardSteps.tsx` - مؤشر خطوات المعالج (4 خطوات)
- `PaymentPlanBuilder.tsx` - بناء جدول الدفعات
- `BookAppointmentDialog.tsx` - حجز موعد للعميل

#### مكونات العملاء (Leads/Clients)
- `LeadDetailDrawer.tsx` - لوحة تفاصيل العميل الجديد
- `TourCard.tsx` - بطاقة الجولة/الزيارة

#### مكونات المالية (Finance)
- `PaymentLinkModal.tsx` - إنشاء رابط الدفع
- `TransactionFeed.tsx` - سجل المعاملات المالية

#### مكونات النظام (System)
- `PermissionGuard.tsx` - حماية الصلاحيات
- `SystemHealthBar.tsx` - حالة النظام

#### مكونات UI الأساسية
- `ui/badge.tsx`
- `ui/button.tsx`
- `ui/card.tsx`
- `ui/input.tsx`

### المجلدات الرئيسية

```
mavera_admin_new/
├── app/                    # Next.js App Router
│   ├── admin/             # صفحات الإدارة
│   ├── sales/              # صفحات المبيعات
│   ├── api/                # API Routes
│   └── [pages]/            # صفحات أخرى
├── components/             # المكونات القابلة لإعادة الاستخدام
│   └── ui/                 # مكونات UI الأساسية
├── lib/                    # المكتبات والمساعدات
│   ├── auth.tsx            # نظام المصادقة والصلاحيات
│   ├── i18n.tsx            # الترجمة (عربي/إنجليزي)
│   └── utils.ts            # دوال مساعدة
├── services/               # خدمات خارجية
│   └── geminiService.ts    # خدمة AI للتحليل
├── types/                  # تعريفات TypeScript
│   ├── auth.ts             # أنواع المستخدمين والصلاحيات
│   └── types.ts            # أنواع البيانات العامة
└── metadata.json           # معلومات المشروع
```

### ملفات API Routes

**عدد ملفات API:** 1 ملف

- `/app/api/ai/analyze/route.ts` - تحليل المخاطر باستخدام Google Gemini AI

---

## 📄 2. تفاصيل كل صفحة

### 2.1 الصفحة الرئيسية (`/`)

**الهدف:** إعادة التوجيه التلقائي حسب دور المستخدم

**المكونات المستخدمة:**
- `useAuth` hook
- `useRouter` من Next.js

**المنطق:**
- `SUPER_ADMIN` → `/admin`
- `SALES_AGENT` → `/sales-dashboard`
- `CALL_CENTER` → `/leads`
- `FINANCE_MANAGER` → `/admin/finance`
- افتراضي → `/sales-dashboard`

**State Management:** لا يوجد (إعادة توجيه فقط)

---

### 2.2 لوحة تحكم الإدارة (`/admin`)

**الهدف:** نظرة شمولية على أداء النظام

**المكونات المستخدمة:**
- `SystemHealthBar` - حالة النظام
- KPIs Cards (Revenue, Pipeline, NPS)
- Recent Activity Feed
- Action Center

**الميزات:**
- عرض الإيرادات الشهرية (845,000 SAR)
- قيمة الصفقات المحتملة (1.2M SAR)
- مؤشر رضا العملاء (NPS: 9.2/10)
- سجل النشاط الأخير
- روابط سريعة (دعوة موظف، التقارير)

**State Management:** لا يوجد (عرض بيانات ثابتة)

**API Calls:** لا يوجد (Mock Data)

---

### 2.3 لوحة المالية (`/admin/finance`)

**الهدف:** إدارة العمليات المالية والتحصيل

**المكونات المستخدمة:**
- `TransactionFeed` - سجل المعاملات
- `PermissionGuard` - حماية الصلاحيات

**الميزات:**
- التحصيل اليومي (45,200 SAR)
- الفواتير المعلقة (128,500 SAR)
- حالة الربط مع ERP (Oracle NetSuite)
- إجراءات سريعة (إنشاء فاتورة يدوية، تسوية المدفوعات)

**State Management:** لا يوجد

**API Calls:** لا يوجد (Mock Data)

**التكاملات:**
- Oracle NetSuite ERP
- Riyad Bank Payment Gateway

---

### 2.4 إدارة الفواتير (`/admin/finance/invoices`)

**الهدف:** إدارة الفواتير والمدفوعات

**المكونات المستخدمة:**
- `PaymentLinkModal` - إنشاء رابط الدفع
- `PermissionGuard` - حماية الصلاحيات

**الميزات:**
- جدول الفواتير مع التصفية (All, Unpaid, Paid, Overdue, Draft)
- حالة المزامنة مع ERP
- إنشاء رابط دفع (Payment Link)
- تحميل PDF
- بحث عن الفواتير

**State Management:**
- `activeTab` - التبويب النشط
- `selectedInvoice` - الفاتورة المحددة

**API Calls:** لا يوجد (Mock Data)

**التكاملات:**
- Riyad Bank Payment Gateway (لإنشاء روابط الدفع)

---

### 2.5 أرشيف العقود (`/admin/finance/contracts`)

**الهدف:** إدارة العقود والإضافات (Add-ons)

**الميزات:**
- عرض العقود مع حالة (Active, Refunded)
- عرض الإضافات والتعديلات (Nested View)
- البحث عن العقود
- تحميل نسخة قانونية

**State Management:**
- `expandedRow` - الصف المفتوح لعرض الإضافات

**API Calls:** لا يوجد (Mock Data)

---

### 2.6 التكاملات الخارجية (`/admin/finance/integrations`)

**الهدف:** مراقبة حالة التكاملات الخارجية

**الأنظمة المتكاملة:**
1. **Oracle NetSuite** (ERP/Accounting) - Healthy
2. **Riyad Bank Gateway** (Payment Gateway) - Healthy
3. **Nafath** (National IAM) - Service Issues

**الميزات:**
- عرض حالة كل نظام
- آخر مزامنة
- زر اختبار الاتصال (للمستخدمين المصرح لهم)

**State Management:** لا يوجد

**API Calls:** لا يوجد (Mock Data)

---

### 2.7 لوحة تحكم المبيعات (`/sales-dashboard`)

**الهدف:** نظرة عامة على أداء المبيعات

**الميزات:**
- الهدف الشهري (342k / 500k = 68%)
- عدد الصفقات النشطة (28)
- الجولات المجدولة (4)
- قائمة الجولات القادمة

**State Management:** لا يوجد

**API Calls:** لا يوجد (Mock Data)

---

### 2.8 معالج الحجز الجديد (`/sales/new`)

**الهدف:** إنشاء حجز جديد عبر معالج من 4 خطوات

**المكونات المستخدمة:**
- `BookingWizardSteps` - مؤشر الخطوات
- `PaymentPlanBuilder` - بناء جدول الدفعات

**الخطوات:**

**الخطوة 1: الفعالية والقاعة**
- اختيار تاريخ الفعالية
- عدد الضيوف
- اختيار القاعة (Mavera Hall A/B, Grand Garden)

**الخطوة 2: التسعير والخدمات**
- السعر الأساسي
- نسبة الخصم (مع تحذير إذا > 5%)
- الخدمات الإضافية (Coffee Service, VIP Dinner, etc.)

**الخطوة 3: جدول الدفعات**
- بناء جدول الدفعات (Deposit, Installments)
- السماح بالدفع المقسم
- التحقق من أن النسب = 100%

**الخطوة 4: العقد والحساب**
- مراجعة الملخص
- إنشاء الحساب والعقد
- إرسال إلى Nafath للتوقيع

**State Management:**
- `step` - الخطوة الحالية (1-4)
- `eventDate`, `hall`, `guests` - بيانات الفعالية
- `totalPackagePrice`, `discountPercent`, `finalPrice` - التسعير
- `installments` - جدول الدفعات
- `hasSplitPayment` - السماح بالدفع المقسم
- `aiAnalysis` - نتيجة تحليل AI

**API Calls:**
- `/api/ai/analyze` - تحليل المخاطر باستخدام AI

**التكاملات:**
- Google Gemini AI (لتحليل المخاطر)
- Nafath (للتوقيع الإلكتروني)

---

### 2.9 مراحل البيع (`/sales/pipeline`)

**الهدف:** تتبع الصفقات عبر مراحل البيع (Kanban Board)

**المراحل:**
1. **New Lead** - عميل جديد
2. **Tour Scheduled** - جولة مجدولة
3. **Proposal Sent** - تم إرسال العرض
4. **Pending Nafath** - بانتظار نفاذ
5. **Won** - تم التوقيع والدفع

**الميزات:**
- عرض الصفقات في كل مرحلة
- معلومات العميل والقيمة
- تاريخ الفعالية

**State Management:** لا يوجد (Mock Data)

**API Calls:** لا يوجد

---

### 2.10 إدارة الجولات (`/sales/tours`)

**الهدف:** جدولة وإدارة الزيارات الميدانية

**المكونات المستخدمة:**
- `TourCard` - بطاقة الجولة

**الميزات:**
- التصفية (All, Today, Upcoming, Completed)
- البحث عن العملاء
- تحديث حالة الجولة (Scheduled, Completed, NoShow)
- إنشاء عرض سعر من الجولة

**State Management:**
- `activeTab` - التبويب النشط
- `tours` - قائمة الجولات

**API Calls:** لا يوجد (Mock Data)

---

### 2.11 إدارة العملاء الجدد (`/leads`)

**الهدف:** إدارة الاستفسارات وجدولة المواعيد

**المكونات المستخدمة:**
- `BookAppointmentDialog` - حجز موعد
- `LeadDetailDrawer` - لوحة تفاصيل العميل

**الميزات:**
- التصفية حسب الحالة (New, Follow Up, Booked/Closed)
- البحث السريع
- عرض مصدر العميل (Instagram, Google, Referral)
- حجز موعد مع موظف مبيعات
- سجل التفاعلات
- إضافة ملاحظات

**State Management:**
- `activeTab` - التبويب النشط
- `selectedLead` - العميل المحدد
- `isDialogOpen` - حالة نافذة الحجز

**API Calls:** لا يوجد (Mock Data)

---

### 2.12 إدارة الموظفين (`/users`)

**الهدف:** إدارة حسابات المستخدمين والصلاحيات

**الميزات:**
- عرض قائمة الموظفين
- البحث عن الموظفين
- تصفية حسب الدور
- دعوة موظف جديد (Modal)
- حالة النشاط (Active/Inactive)
- آخر تسجيل دخول

**State Management:**
- `isInviteOpen` - حالة نافذة الدعوة
- `users` - قائمة الموظفين

**API Calls:** لا يوجد (Mock Data)

---

### 2.13 موافقات الخصم (`/approvals`)

**الهدف:** مراجعة طلبات الخصم التي تتجاوز الحد المسموح (>5%)

**الميزات:**
- عرض الطلبات المعلقة
- تفاصيل الطلب (السعر الأصلي، المطلوب، نسبة الخصم)
- سبب الطلب
- الموافقة أو الرفض
- سجل القرارات السابقة

**State Management:**
- `requests` - قائمة الطلبات

**API Calls:** لا يوجد (Mock Data)

---

### 2.14 التقارير والتحليلات (`/reports`)

**الهدف:** مؤشرات الأداء المالي والتشغيلي

**المكونات المستخدمة:**
- `recharts` - مكتبة الرسوم البيانية

**الميزات:**
- رسم بياني للإيرادات (Area Chart)
- رسم دائري لمصادر العملاء (Pie Chart)
- إحصائيات (Conversion Rate, Avg Deal Size, Total Events, Pending Collections)
- تصدير PDF
- تصفية حسب الفترة

**State Management:** لا يوجد (Mock Data)

**API Calls:** لا يوجد

---

### 2.15 سجل العملاء (`/clients`)

**الهدف:** تتبع حالة العقود والدفعات والترتيبات

**الميزات:**
- عرض جميع العملاء
- حالة العقد (Draft, Signed, Pending)
- حالة المدفوعات (Paid, Partial, Overdue)
- حالة الترتيبات (Pending, Uploaded)
- البحث عن العملاء

**State Management:**
- `clients` - قائمة العملاء

**API Calls:** لا يوجد (Mock Data)

---

### 2.16 الترتيبات والتنفيذ (`/arrangements`)

**الهدف:** رفع المستندات النهائية لبدء التنفيذ

**الميزات:**
- قائمة الحجوزات المؤكدة
- رفع ملف PDF (الترتيبات النهائية)
- تحديث الحالة إلى "Ready for Execution"
- إشعار العميل عبر WhatsApp

**State Management:**
- `searchTerm` - مصطلح البحث
- `selectedBooking` - الحجز المحدد
- `isUploading` - حالة الرفع
- `uploadComplete` - اكتمال الرفع

**API Calls:** لا يوجد (Mock Data)

**التكاملات:**
- WhatsApp (لإشعار العملاء)

---

## 🧩 3. تفاصيل المكونات (Components)

### 3.1 مكونات التخطيط

#### `Sidebar.tsx`
**الوظيفة:** القائمة الجانبية مع تصفية حسب الدور

**Props:**
- `userRole: UserRole` - دور المستخدم
- `isOpen: boolean` - حالة الفتح/الإغلاق
- `onClose: () => void` - دالة الإغلاق

**الميزات:**
- تصفية العناصر حسب الدور
- دعم RTL/LTR
- تصميم متجاوب (Responsive)
- علامة الصفحة النشطة

**Dependencies:**
- `next/navigation` (useRouter, usePathname)
- `lib/i18n` (useLanguage)
- `types/auth` (UserRole)

---

#### `Header.tsx`
**الوظيفة:** شريط التنقل العلوي

**Props:**
- `user: UserProfile` - بيانات المستخدم
- `onRoleSwitch: (role: UserRole) => void` - تبديل الدور
- `onToggleSidebar: () => void` - فتح/إغلاق القائمة

**الميزات:**
- Breadcrumb ديناميكي
- بحث (Placeholder)
- تبديل اللغة (عربي/إنجليزي)
- عرض الدور الحالي (View As)
- معلومات المستخدم

**Dependencies:**
- `next/navigation` (usePathname)
- `lib/i18n` (useLanguage)
- `types/auth` (UserRole, UserProfile)

---

### 3.2 مكونات الحجوزات

#### `BookingWizardSteps.tsx`
**الوظيفة:** مؤشر خطوات المعالج

**Props:**
- `currentStep: number` - الخطوة الحالية (1-4)

**الميزات:**
- خط تقدم متحرك
- علامات الخطوات المكتملة
- دعم RTL/LTR

**Dependencies:**
- `lib/i18n` (useLanguage)

---

#### `PaymentPlanBuilder.tsx`
**الوظيفة:** بناء جدول الدفعات

**Props:**
- `installments: PaymentInstallment[]` - قائمة الدفعات
- `totalAmount: number` - المبلغ الإجمالي
- `onUpdate: (installments) => void` - تحديث الدفعات
- `hasSplitPayment: boolean` - السماح بالدفع المقسم
- `onToggleSplitPayment: (value) => void` - تبديل الدفع المقسم

**الميزات:**
- إضافة/حذف دفعات
- حساب المبلغ تلقائياً حسب النسبة
- التحقق من أن النسب = 100%
- جدول تفاعلي

**Dependencies:**
- `types` (PaymentInstallment)
- `lib/i18n` (useLanguage)

---

#### `BookAppointmentDialog.tsx`
**الوظيفة:** حجز موعد للعميل

**Props:**
- `isOpen: boolean` - حالة الفتح
- `onClose: () => void` - دالة الإغلاق
- `leadName: string` - اسم العميل

**الميزات:**
- اختيار موظف المبيعات
- اختيار التاريخ والوقت
- شبكة الأوقات المتاحة
- تأكيد الحجز

**Dependencies:**
- `lib/i18n` (useLanguage)

---

### 3.3 مكونات العملاء

#### `LeadDetailDrawer.tsx`
**الوظيفة:** لوحة تفاصيل العميل الجديد

**Props:**
- `isOpen: boolean` - حالة الفتح
- `onClose: () => void` - دالة الإغلاق
- `lead: Lead | null` - بيانات العميل
- `onBookVisit: () => void` - دالة حجز زيارة

**الميزات:**
- معلومات الاتصال
- سجل التفاعلات (Timeline)
- إضافة ملاحظات
- إجراءات سريعة (حجز موعد، اتصال)

**Dependencies:**
- `lib/i18n` (useLanguage)

---

#### `TourCard.tsx`
**الوظيفة:** بطاقة الجولة/الزيارة

**Props:**
- `tour: Tour` - بيانات الجولة
- `onStatusChange: (id, status) => void` - تحديث الحالة

**الميزات:**
- عرض معلومات الجولة
- تحديث الحالة (Scheduled, Completed, NoShow)
- إنشاء عرض سعر من الجولة

**Dependencies:**
- `lib/i18n` (useLanguage)
- `next/navigation` (useRouter)

---

### 3.4 مكونات المالية

#### `PaymentLinkModal.tsx`
**الوظيفة:** إنشاء رابط الدفع

**Props:**
- `isOpen: boolean` - حالة الفتح
- `onClose: () => void` - دالة الإغلاق
- `invoice: any` - بيانات الفاتورة

**الميزات:**
- محاكاة الاتصال بـ Payment Gateway
- عرض رابط الدفع
- نسخ الرابط
- إرسال عبر WhatsApp

**Dependencies:** لا يوجد

---

#### `TransactionFeed.tsx`
**الوظيفة:** سجل المعاملات المالية

**الميزات:**
- عرض آخر المعاملات
- حالة المعاملة (Success, Pending)
- طريقة الدفع
- رابط "عرض الكل"

**Dependencies:**
- `lib/i18n` (useLanguage)

---

### 3.5 مكونات النظام

#### `PermissionGuard.tsx`
**الوظيفة:** حماية الصلاحيات

**Props:**
- `permission: Permission` - الصلاحية المطلوبة
- `children: React.ReactNode` - المحتوى
- `fallback?: React.ReactNode` - المحتوى البديل

**الميزات:**
- إخفاء المحتوى إذا لم تكن الصلاحية متوفرة
- عرض محتوى بديل (اختياري)

**Dependencies:**
- `lib/auth` (usePermission)
- `types/auth` (Permission)

---

#### `SystemHealthBar.tsx`
**الوظيفة:** عرض حالة النظام

**الميزات:**
- حالة قاعدة البيانات
- حالة API
- حالة الأمان

**Dependencies:**
- `lib/i18n` (useLanguage)

---

## 👥 4. أنواع المستخدمين (User Roles)

### 4.1 الأدوار المتاحة

**إجمالي الأدوار:** 5 أدوار

1. **SUPER_ADMIN** - المدير العام
2. **FINANCE_MANAGER** - مدير المالية
3. **SALES_AGENT** - موظف مبيعات
4. **CALL_CENTER** - مركز الاتصال
5. **COORDINATOR** - منسق

### 4.2 الصلاحيات لكل دور

#### SUPER_ADMIN
**الصلاحيات:**
- `finance.view`
- `finance.manage_invoices`
- `finance.create_invoice`
- `finance.sync_erp`
- `finance.view_contracts`
- `finance.manage_integrations`
- `sales.view`
- `sales.create_quote`
- `leads.view`
- `leads.manage`

**الصفحات المتاحة:**
- جميع الصفحات

---

#### FINANCE_MANAGER
**الصلاحيات:**
- `finance.view`
- `finance.manage_invoices`
- `finance.create_invoice`
- `finance.sync_erp`
- `finance.view_contracts`
- `finance.manage_integrations`

**الصفحات المتاحة:**
- `/admin/finance`
- `/admin/finance/invoices`
- `/admin/finance/contracts`
- `/admin/finance/integrations`

---

#### SALES_AGENT
**الصلاحيات:**
- `sales.view`
- `sales.create_quote`
- `leads.view`

**الصفحات المتاحة:**
- `/sales-dashboard`
- `/sales/new`
- `/sales/pipeline`
- `/sales/tours`
- `/leads` (عرض فقط)

---

#### CALL_CENTER
**الصلاحيات:**
- `leads.view`
- `leads.manage`

**الصفحات المتاحة:**
- `/leads`

---

#### COORDINATOR
**الصلاحيات:**
- `finance.view_contracts`

**الصفحات المتاحة:**
- `/arrangements`
- `/admin/finance/contracts` (عرض فقط)

---

## 🔄 5. الـ Workflow & User Flow

### 5.1 رحلة العميل الجديد (Lead Journey)

1. **استلام الاستفسار** (`/leads`)
   - العميل يملأ نموذج على الموقع
   - أو اتصال هاتفي
   - يتم إنشاء Lead جديد

2. **التواصل الأولي**
   - Call Center يتصل بالعميل
   - إضافة ملاحظات في `LeadDetailDrawer`

3. **حجز موعد زيارة**
   - استخدام `BookAppointmentDialog`
   - اختيار موظف مبيعات
   - اختيار التاريخ والوقت

4. **الزيارة الميدانية** (`/sales/tours`)
   - موظف المبيعات يذهب للزيارة
   - تحديث الحالة (Completed/NoShow)

5. **إنشاء عرض سعر** (`/sales/new`)
   - الخطوة 1: اختيار القاعة والتاريخ
   - الخطوة 2: التسعير والخدمات
   - الخطوة 3: جدول الدفعات
   - الخطوة 4: إنشاء العقد

6. **إرسال العقد**
   - إرسال عبر Nafath للتوقيع الإلكتروني

7. **التوقيع والدفع**
   - العميل يوقع عبر Nafath
   - الدفع عبر Payment Gateway

8. **الترتيبات** (`/arrangements`)
   - رفع المستندات النهائية
   - تحديث الحالة إلى "Ready for Execution"

---

### 5.2 حالات الحجز (Booking Status)

**Enum:** `BookingStatus`

1. **DRAFT** - مسودة
2. **PENDING_APPROVAL** - بانتظار الموافقة (إذا الخصم > 5%)
3. **CONFIRMED** - مؤكد (تم التوقيع والدفع)
4. **READY_FOR_EXECUTION** - جاهز للتنفيذ (تم رفع الترتيبات)
5. **COMPLETED** - مكتمل

---

### 5.3 رحلة الموافقة على الخصم

1. **إنشاء عرض سعر** (`/sales/new`)
   - إذا الخصم > 5% → يحتاج موافقة

2. **إرسال للموافقة** (`/approvals`)
   - يظهر في قائمة الطلبات المعلقة
   - مع تفاصيل (السعر الأصلي، المطلوب، السبب)

3. **المراجعة**
   - SUPER_ADMIN يراجع الطلب
   - الموافقة أو الرفض

4. **التحديث**
   - يتم تحديث حالة الحجز
   - إشعار موظف المبيعات

---

## 🔗 6. التكاملات والاتصالات

### 6.1 APIs خارجية

#### Google Gemini AI
**الاستخدام:** تحليل مخاطر العروض
**Endpoint:** `/api/ai/analyze`
**المدخلات:**
- `totalAmount: number`
- `discount: number`
- `installments: PaymentInstallment[]`

**المخرجات:**
- `riskLevel: "LOW" | "MEDIUM" | "HIGH"`
- `recommendation: string`

**Fallback:** إذا فشل AI، يتم استخدام قاعدة منطقية بسيطة

---

#### Oracle NetSuite (ERP)
**الاستخدام:** مزامنة الفواتير والمدفوعات
**الحالة:** Healthy (آخر مزامنة: 2 دقائق)

**الوظائف:**
- مزامنة الفواتير
- مزامنة المدفوعات
- تسوية البيانات

---

#### Riyad Bank Payment Gateway
**الاستخدام:** إنشاء روابط الدفع
**الحالة:** Healthy

**الوظائف:**
- إنشاء رابط دفع للفاتورة
- استلام إشعارات الدفع
- دعم طرق الدفع (Mada, Visa, Mastercard, Bank Transfer)

---

#### Nafath (National IAM)
**الاستخدام:** التوقيع الإلكتروني
**الحالة:** Service Issues (مشاكل في الخدمة)

**الوظائف:**
- إرسال العقد للتوقيع
- التحقق من الهوية
- استلام العقد الموقع

---

### 6.2 Database Schema/Models

**ملاحظة:** لا يوجد تعريف واضح لـ Database Schema في الكود الحالي. البيانات هي Mock Data.

**الأنواع المتوقعة:**

```typescript
// User
interface User {
  id: string;
  name: string;
  email: string;
  role: UserRole;
  status: 'Active' | 'Inactive';
  lastLogin: string;
}

// Lead
interface Lead {
  id: string;
  name: string;
  phone: string;
  source: 'Instagram' | 'Google' | 'Referral';
  status: 'New' | 'Contacted' | 'Booked';
  date: string;
  notes?: string;
}

// Booking
interface Booking {
  id: string;
  clientName: string;
  eventDate: string;
  packagePrice: number;
  discountPercentage: number;
  finalPrice: number;
  status: BookingStatus;
  installments: PaymentInstallment[];
  hasSplitPayment: boolean;
  contractPdfUrl?: string;
  coordinatorNotes?: string;
}

// Invoice
interface Invoice {
  id: string;
  client: string;
  amount: number;
  due: string;
  status: 'Paid' | 'Unpaid' | 'Overdue' | 'Draft';
  method: string;
  erp: 'Synced' | 'Pending';
}
```

---

### 6.3 Authentication & Authorization

**نظام المصادقة:**
- حالياً: Mock Users (لا يوجد نظام مصادقة حقيقي)
- الملف: `lib/auth.tsx`

**نظام الصلاحيات:**
- Role-Based Access Control (RBAC)
- Permission-Based Access Control (PBAC)
- الملف: `types/auth.ts`

**الصلاحيات المتاحة:**
```typescript
type Permission = 
  | 'finance.view'
  | 'finance.manage_invoices'
  | 'finance.create_invoice'
  | 'finance.sync_erp'
  | 'finance.view_contracts'
  | 'finance.manage_integrations'
  | 'sales.view'
  | 'sales.create_quote'
  | 'leads.view'
  | 'leads.manage';
```

**المكونات:**
- `AuthProvider` - Context Provider
- `useAuth` - Hook للوصول للمستخدم
- `usePermission` - Hook للتحقق من الصلاحية
- `PermissionGuard` - Component لحماية المحتوى

---

### 6.4 File Upload/Storage

**الاستخدام:**
- رفع ملفات الترتيبات (`/arrangements`)
- تحميل PDF للعقود والفواتير

**الملفات المدعومة:**
- PDF (للعقود والترتيبات)

**الحالة الحالية:**
- محاكاة فقط (لا يوجد رفع حقيقي)

---

## ⚙️ 7. الميزات الوظيفية الموجودة

### 7.1 إدارة الحجوزات ✅

**الميزات:**
- معالج الحجز (4 خطوات)
- اختيار القاعة والتاريخ
- التسعير والخصومات
- جدول الدفعات
- إنشاء العقد

**الحالة:** مكتمل (مع Mock Data)

---

### 7.2 نظام الدفع ✅

**الميزات:**
- جدول الدفعات القابل للتخصيص
- الدفع المقسم
- إنشاء روابط الدفع
- تتبع المدفوعات
- مزامنة مع ERP

**الحالة:** مكتمل (مع Mock Data)

---

### 7.3 العقود والتوقيع الإلكتروني ⚠️

**الميزات:**
- إنشاء العقد
- إرسال إلى Nafath
- أرشيف العقود
- تتبع الإضافات والتعديلات

**الحالة:** جزئي (التكامل مع Nafath غير مكتمل)

---

### 7.4 بوابة العملاء ❌

**الحالة:** غير موجودة

**ملاحظة:** النظام الحالي هو لوحة تحكم إدارية فقط. لا توجد بوابة عملاء منفصلة.

---

### 7.5 لوحات التحكم (Dashboards) ✅

**الميزات:**
- لوحة تحكم الإدارة (`/admin`)
- لوحة تحكم المبيعات (`/sales-dashboard`)
- لوحة تحكم المالية (`/admin/finance`)

**الحالة:** مكتمل

---

### 7.6 الإشعارات (Notifications) ⚠️

**الميزات:**
- إشعارات WhatsApp (محاكاة فقط)
- إشعارات داخل النظام (Recent Activity)

**الحالة:** جزئي (محاكاة فقط)

---

### 7.7 ميزات أخرى ✅

**AI Risk Analysis:**
- تحليل مخاطر العروض باستخدام Google Gemini AI
- تقييم الخصومات وجداول الدفعات

**Multi-language Support:**
- دعم العربية والإنجليزية
- RTL/LTR Support

**Permission System:**
- نظام صلاحيات متقدم
- حماية على مستوى المكونات

**Responsive Design:**
- تصميم متجاوب لجميع الأجهزة

---

## 📊 8. حالة المشروع

### 8.1 الأقسام المكتملة ✅

1. **هيكل المشروع الأساسي**
   - Next.js App Router
   - TypeScript
   - Tailwind CSS

2. **نظام المصادقة والصلاحيات**
   - Role-Based Access Control
   - Permission-Based Access Control
   - PermissionGuard Component

3. **التصميم والواجهة**
   - تصميم متجاوب
   - دعم RTL/LTR
   - نظام الألوان (Mavera Brand)

4. **الصفحات الأساسية**
   - جميع الصفحات موجودة
   - التخطيط الأساسي مكتمل

5. **المكونات الأساسية**
   - جميع المكونات موجودة
   - الوظائف الأساسية تعمل

6. **نظام الترجمة**
   - دعم العربية والإنجليزية
   - جميع النصوص مترجمة

---

### 8.2 الأقسام قيد التطوير ⚠️

1. **التكاملات الخارجية**
   - Oracle NetSuite (محاكاة فقط)
   - Riyad Bank Gateway (محاكاة فقط)
   - Nafath (محاكاة فقط)

2. **قاعدة البيانات**
   - لا يوجد اتصال بقاعدة بيانات حقيقية
   - جميع البيانات Mock Data

3. **نظام المصادقة**
   - لا يوجد نظام مصادقة حقيقي
   - Mock Users فقط

4. **رفع الملفات**
   - محاكاة فقط
   - لا يوجد تخزين حقيقي

5. **الإشعارات**
   - محاكاة فقط
   - لا يوجد تكامل حقيقي مع WhatsApp

---

### 8.3 الأقسام المفقودة أو الـ Placeholders ❌

1. **بوابة العملاء**
   - غير موجودة
   - يحتاج تطوير منفصل

2. **نظام التقارير المتقدم**
   - الرسوم البيانية موجودة (Mock Data)
   - يحتاج تكامل مع بيانات حقيقية

3. **نظام الإشعارات المتقدم**
   - إشعارات داخلية
   - إشعارات عبر البريد الإلكتروني
   - إشعارات Push

4. **نظام البحث المتقدم**
   - البحث الحالي Placeholder فقط
   - يحتاج تكامل مع قاعدة بيانات

5. **نظام التقويم**
   - `/calendar` موجود في القائمة لكن الصفحة غير موجودة

6. **نظام الإعدادات**
   - `/settings` موجود في القائمة لكن الصفحة غير موجودة

---

## 🛠️ 9. التقنيات المستخدمة

### 9.1 Next.js

**الإصدار:** 14.1.0

**الميزات المستخدمة:**
- **App Router** (ليس Pages Router)
- **Server Components** (في بعض الأماكن)
- **Client Components** (`"use client"`)
- **API Routes** (`/app/api/`)

---

### 9.2 UI Libraries

#### Tailwind CSS ✅
**الإصدار:** 3.3.0

**التخصيص:**
- ألوان مخصصة (Mavera Brand)
- Shadows مخصصة
- Animations مخصصة

**الملفات:**
- `tailwind.config.ts`
- `app/globals.css`

---

#### Lucide React ✅
**الإصدار:** 0.344.0

**الاستخدام:** الأيقونات في جميع أنحاء التطبيق

---

#### Recharts ✅
**الإصدار:** 2.12.0

**الاستخدام:** الرسوم البيانية في صفحة التقارير

---

### 9.3 State Management

**الحالة الحالية:** React Context API + Local State

**المستخدم:**
- `AuthContext` - للمصادقة والصلاحيات
- `LanguageContext` - للترجمة
- `useState` - للحالة المحلية في المكونات

**ملاحظة:** لا يوجد Redux أو Zustand أو أي State Management Library أخرى.

---

### 9.4 Form Handling

**الحالة الحالية:** Controlled Components (React State)

**المستخدم:**
- `useState` للتحكم في القيم
- Validation يدوي

**ملاحظة:** لا يوجد React Hook Form أو Formik.

---

### 9.5 Other Libraries

#### Google GenAI ✅
**الإصدار:** 1.30.0

**الاستخدام:** تحليل مخاطر العروض

---

### 9.6 TypeScript

**الإصدار:** 5.x

**الإعدادات:**
- `strictNullChecks: true`
- `strict: false`
- `jsx: "preserve"`

---

## 📝 10. ملاحظات إضافية

### 10.1 Patterns و Best Practices

**✅ الإيجابيات:**

1. **TypeScript Usage**
   - استخدام TypeScript في جميع الملفات
   - تعريفات واضحة للأنواع

2. **Component Organization**
   - فصل المكونات حسب الوظيفة
   - مكونات قابلة لإعادة الاستخدام

3. **Permission System**
   - نظام صلاحيات متقدم
   - حماية على مستوى المكونات

4. **Internationalization**
   - دعم متعدد اللغات
   - RTL/LTR Support

5. **Responsive Design**
   - تصميم متجاوب
   - Mobile-First Approach

6. **Code Organization**
   - هيكل مجلدات واضح
   - فصل الاهتمامات (Separation of Concerns)

---

**⚠️ نقاط التحسين:**

1. **State Management**
   - استخدام Context API فقط قد يكون غير كافٍ للمشاريع الكبيرة
   - قد يحتاج Zustand أو Redux Toolkit

2. **Form Handling**
   - استخدام React Hook Form سيكون أفضل
   - Validation أكثر قوة

3. **Error Handling**
   - لا يوجد Error Boundaries
   - لا يوجد معالجة للأخطاء في API Calls

4. **Loading States**
   - بعض الصفحات لا تحتوي على Loading States
   - قد يحتاج Skeleton Loaders

5. **Data Fetching**
   - لا يوجد React Query أو SWR
   - جميع البيانات Mock Data

---

### 10.2 Code Quality & Organization

**✅ القوة:**

1. **Type Safety**
   - TypeScript في جميع الملفات
   - تعريفات واضحة للأنواع

2. **Component Reusability**
   - مكونات قابلة لإعادة الاستخدام
   - Props واضحة

3. **Consistent Styling**
   - استخدام Tailwind CSS بشكل متسق
   - نظام ألوان موحد

4. **Clean Code**
   - كود نظيف وواضح
   - أسماء متغيرات وصفية

---

**⚠️ التحسينات المطلوبة:**

1. **Testing**
   - لا يوجد Unit Tests
   - لا يوجد Integration Tests
   - لا يوجد E2E Tests

2. **Documentation**
   - لا يوجد JSDoc Comments
   - لا يوجد Storybook

3. **Performance**
   - لا يوجد Code Splitting واضح
   - قد يحتاج Lazy Loading

4. **Accessibility**
   - قد يحتاج تحسينات في Accessibility
   - ARIA Labels

---

### 10.3 Technical Debt

1. **Mock Data**
   - جميع البيانات Mock Data
   - يحتاج تكامل مع Backend حقيقي

2. **Authentication**
   - لا يوجد نظام مصادقة حقيقي
   - Mock Users فقط

3. **API Integration**
   - التكاملات الخارجية محاكاة فقط
   - يحتاج تكامل حقيقي

4. **File Upload**
   - رفع الملفات محاكاة فقط
   - يحتاج تخزين حقيقي (S3, Cloudinary, etc.)

5. **Error Handling**
   - لا يوجد معالجة للأخطاء
   - يحتاج Error Boundaries

---

### 10.4 نقاط القوة في الكود الحالي

1. **Architecture**
   - هيكل واضح ومنظم
   - فصل الاهتمامات

2. **Type Safety**
   - TypeScript في جميع الملفات
   - تعريفات واضحة

3. **UI/UX**
   - تصميم احترافي
   - تجربة مستخدم جيدة

4. **Scalability**
   - هيكل قابل للتوسع
   - مكونات قابلة لإعادة الاستخدام

5. **Internationalization**
   - دعم متعدد اللغات
   - RTL/LTR Support

6. **Permission System**
   - نظام صلاحيات متقدم
   - حماية على مستوى المكونات

---

## 📋 ملخص تنفيذي

### الإحصائيات

- **عدد الصفحات:** 15 صفحة
- **عدد المكونات:** 15 مكون
- **عدد الأدوار:** 5 أدوار
- **عدد الصلاحيات:** 10 صلاحيات
- **عدد التكاملات:** 4 تكاملات خارجية

### الحالة العامة

**✅ المكتمل:**
- هيكل المشروع الأساسي
- جميع الصفحات والمكونات
- نظام الصلاحيات
- نظام الترجمة
- التصميم والواجهة

**⚠️ قيد التطوير:**
- التكاملات الخارجية
- قاعدة البيانات
- نظام المصادقة
- رفع الملفات

**❌ المفقود:**
- بوابة العملاء
- نظام التقويم
- نظام الإعدادات
- Testing
- Documentation

### التوصيات

1. **الأولوية العالية:**
   - تكامل مع Backend حقيقي
   - نظام مصادقة حقيقي
   - قاعدة بيانات

2. **الأولوية المتوسطة:**
   - React Query للـ Data Fetching
   - React Hook Form للـ Forms
   - Error Handling

3. **الأولوية المنخفضة:**
   - Testing
   - Documentation
   - Performance Optimization

---

**تم إعداد التقرير بواسطة:** AI Assistant  
**التاريخ:** 2024

